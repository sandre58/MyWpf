<pages:AutoBuildPage x:Class="MyNet.Avalonia.Demo.Pages.BannersPage"
                     xmlns="https://github.com/avaloniaui"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:my="http://mynet.com/avalonia"
                     xmlns:pages="clr-namespace:MyNet.Avalonia.Demo.Pages"
                     ClipToBounds="False"
                     mc:Ignorable="d">

    <DockPanel>

        <!-- Settings -->
        <HeaderedContentControl Classes="Settings" DockPanel.Dock="Right">
            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                <StackPanel>
                    <HeaderedContentControl Classes="Labelled Left Regular" Header="{my:Resource ShowClearButton}">
                        <ToggleSwitch Name="ShowClearButton" IsChecked="True" />
                    </HeaderedContentControl>
                    <Button Content="{my:Resource Restore}" my:IconAssist.Icon="{my:Icon Refresh}" Click="Button_Click" />
                </StackPanel>
            </ScrollViewer>
        </HeaderedContentControl>

        <!-- Fields -->
        <ScrollViewer HorizontalScrollBarVisibility="Auto">
            <StackPanel Name="Root" Classes="Large" Grid.IsSharedSizeScope="True" />
        </ScrollViewer>

    </DockPanel>
</pages:AutoBuildPage>